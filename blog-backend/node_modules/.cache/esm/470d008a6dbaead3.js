let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_6db‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_6db‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_6db‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_6db‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_6db‍.w("./api",[["default",["api"],function(v){api=v}]]);_6db‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();








const { PORT, MONGO_URI } = process.env;

mongoose
    .connect(MONGO_URI, { useNewUrlParser: true, useFindAndModify: false })
    .then(() => {
        console.log('Connected to MongoDB');
    })
    .catch((e) => {
        _6db‍.g.console.error(e);
    });

const app = new Koa();
const router = new Router();

router.get('/', (ctx) => {
    ctx.body = '홈';
});

router.get('/about/:name?', (ctx) => {
    const { name } = ctx.params;
    ctx.body = name ? `${name}의 소개` : '소개';
});

router.get('/posts', (ctx) => {
    const { id } = ctx.query;
    ctx.body = id ? `포스트 #${id}` : '포스트가 없습니다.';
});

router.use('/api', api.routes());

app.use(bodyParser());
app.use(jwtMiddleware);

app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
    _6db‍.g.console.log('Listening to port %d', port);
});
